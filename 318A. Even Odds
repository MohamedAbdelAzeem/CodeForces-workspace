
#include <iostream>
#include <vector>
#include <string>

using namespace std;
int main()
{

	long long   n;
	long long   k;
	cin >> n;
	cin >> k;
	if (k <= (n + 1) / 2)
	{
		cout << k * 2 - 1;
	}
	else
	{
		cout << 2 * (k - (n + 1) / 2);

		return 0;
	}
}
